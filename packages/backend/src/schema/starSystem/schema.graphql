type StarSystem implements Positionable {
	id: ID!
	name: String!
	position: Vector!
	owner: Player
}

extend type Game {
	starSystems: [StarSystem!]!
}

type Query {
	starSystem(id: ID!): StarSystem!
}
