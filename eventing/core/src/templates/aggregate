import {
  Aggregate,
  Command,
  CommandInterface,
  CommandRejected,
} from '@eventing/core';

export class $$AggregateName extends Aggregate<{
  
}> {
  initialState() {
    return {
      
    };
  }

  @Command()
  command(
    { events }: CommandInterface,
    arg: string,
  ) {
    if (!arg) {
      throw new CommandRejected('No arg given.');
    }

    events.publish<...>('...', {
      
    });
  }
}
